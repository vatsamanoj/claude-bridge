model_list:
  - model_name: "*"  # 👈 This catches EVERY model name sent by Claude
    litellm_params:
      model: gemini/gemini-2.0-flash-exp
      api_key: "os.environ/GEMINI_API_KEY"
    # This prevents Gemini from crashing when Claude sends 4.5-specific flags
    litellm_settings:
      drop_params: true